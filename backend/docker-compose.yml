version: "3.6"
services:
  mysql:
    image: mcr.microsoft.com/mssql/server:2019-latest
    ports:
      - 1433:1433
    environment:
      SA_PASSWORD: yourStrong(!)Password
      ACCEPT_EULA: Y
    restart: always